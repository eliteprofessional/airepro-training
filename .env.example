# Frontend (support.airepro.in)
FRONTEND_PORT=409
VITE_API_BASE_URL=https://support-s.airepro.in

# Backend (support-s.airepro.in)
BACKEND_PORT=410
HOST=0.0.0.0
PORT=8787
SERVE_FRONTEND=false
CORS_ORIGIN=https://support.airepro.in

# Shared admin password for /admin/login (required for admin CRUD)
ADMIN_PASSWORD=change-me

# Secret used to sign admin JWTs (required)
ADMIN_TOKEN_SECRET=change-me-to-a-long-random-string
